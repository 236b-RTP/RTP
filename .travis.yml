language: ruby
rvm:
  - 2.1.0
services:
  - postgres
before_script:
  - psql -c 'CREATE ROLE rtp NOSUPERUSER NOCREATEDB NOCREATEROLE INHERIT LOGIN; CREATE DATABASE rtp_test WITH OWNER rtp;' -U postgres
script: "RAILS_ENV=test bundle exec rake db:migrate; bundle exec rspec spec"